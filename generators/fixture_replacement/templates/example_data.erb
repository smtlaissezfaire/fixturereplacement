
module FixtureReplacement
<% ActiveRecord::Base.connection.tables.each do |table_name| -%>
<% begin table_name.classify.constantize -%>
<% columns = table_name.classify.constantize.columns -%>
  def <%= table_name.singularize %>_attributes
    {
      # your key-value pairs go here, just like you would feed them
      # into <%= table_name.classify %>.create!().  Use default_<%= table_name.singularize %>
      # for a belongs_to association in another model
		}
	end

<% rescue; end -%>
<% end -%>
end